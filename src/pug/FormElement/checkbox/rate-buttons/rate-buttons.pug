mixin rate-buttons-stars(stars, emty)
    .rate-buttons-stars
        -var i = 1
        while i <= stars
            .rate-buttons-stars__object= i++
        -var i = 1
        while i <= emty
            .rate-buttons-empty__object= i++

